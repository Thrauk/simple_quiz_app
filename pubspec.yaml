name: simple_quizz_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  meta: ^1.3.0
  equatable: ^2.0.3
  flutter_bloc: ^7.2.0
  freezed_annotation: ^0.14.3
  firebase_core: ^1.6.0
  firebase_auth: ^3.1.1
  google_sign_in: ^5.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^0.14.5
  build_runner: ^2.1.2

dependency_overrides:
  meta: ^1.7.0


flutter:
  uses-material-design: true